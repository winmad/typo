<% className = form_action.delete(:class) %>
<%= form_tag(form_action, :id => "#{form_type}_form", :enctype => "multipart/form-data", :class => className) do %>

  <%= render :partial => "form" %>

<% end %>

  <%= render :partial => "merge" unless @article.id.nil? or @article.state.to_s.downcase == 'draft' or User.find(session[:user_id]).profile.label != 'admin' %>
