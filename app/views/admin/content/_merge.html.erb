<br>
<br>
<h4>Merge Articles</h4>
  <div class='action'>
    <%= form_tag url_for(:controller => 'admin/content', :action => 'merge', :id => @article.id), :method => "post" do %>
      <%= label_tag(:merge_with, "Article ID") %>
      <%= text_field_tag :merge_with %><br>
      <%= submit_tag "Merge" %>
    <% end %>
  </div>
